// Do not edit. Auto generated from _wifi_site.html 
#if defined(ARDUINO_RASPBERRY_PI_PICO_W) || defined(ESP32)
  const char indexHTML[] = " <!DOCTYPE html> <html lang='en'>  <head>   <title>GBG CAR</title> </head>  <body>   <p>Use the buttons below to turn the car's joystick on and off.</p>   <button onclick='fetch(\"/activate\");' style='font-size: 100px;'>GO</button>   <button onclick='fetch(\"/deactivate\");' style='font-size: 200px; float: right;'>STOP</button>   <br>   <div id=\"status\" style=\"font-size: 50px;\">status: </div> </body> <script>   async function loop() {     let errorTimeout;     let abortcontroller = new AbortController();     errorTimeout = setTimeout(() => {       abortcontroller.abort(\"aborted because timeout\");     }, 400);     try {       var fetched = await fetch('http://10.0.0.1/status', { signal: abortcontroller.signal });       fetchedResponse = await fetched.text();       document.getElementById(\"status\").innerHTML = \"status: \" + fetchedResponse;     } catch (e) {       document.getElementById(\"status\").innerHTML = \"status: Disconnected\";     }     clearTimeout(errorTimeout);   }   setInterval(loop, 500); </script>  </html>"; 
#endif